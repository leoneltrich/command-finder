[package]
name = "command-finder"
version = "0.1.0"
edition = "2024"

[dependencies]
inquire = "0.7.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rusqlite = { version = "0.32", features = ["bundled"] }
